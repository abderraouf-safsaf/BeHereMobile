<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.teamloosers.behereandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/BeHere">
        <activity
            android:name=".ListEtudiantsActivity"
            android:label="@string/title_activity_list_etudiants"
            android:theme="@style/BeHere"></activity>
        <activity android:name=".ConsultationEtudiantActivity">
        </activity>
        <activity android:name=".NouvelAppelActivity">

        </activity>
        <activity
            android:name=".EtudiantsPresencesActivity"
            android:label="@string/title_activity_etudiants_presences"
            android:theme="@style/BeHere"></activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main_modues"
            android:theme="@style/BeHere">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>